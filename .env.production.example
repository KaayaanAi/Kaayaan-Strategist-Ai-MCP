# Production Environment Configuration
# Copy this to .env.production and update with your actual values

# ==================== Server Configuration ====================
HTTP_MODE=true
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# ==================== Database Configuration ====================
# MongoDB Connection
MONGODB_URI=mongodb://admin:your_strong_password@mongo:27017/kaayaan_strategist?authSource=admin

# Redis Connection  
REDIS_URL=redis://:your_redis_password@redis:6379

# ==================== API Keys ====================
# Alpha Vantage API Key (backup data source)
# Get free API key from: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key_here

# ==================== Application Configuration ====================
# Timezone for timestamps and market hours
TIMEZONE=Asia/Kuwait

# Cache TTL in minutes (affects API call frequency)
CACHE_TTL_MINUTES=15

# Logging level: error, warn, info, debug
LOG_LEVEL=info

# ==================== Rate Limiting ====================
# Yahoo Finance requests per minute (free tier)
YAHOO_RATE_LIMIT=60

# Alpha Vantage requests per minute (free: 5, premium: varies)
ALPHA_VANTAGE_RATE_LIMIT=5

# ==================== Security Configuration ====================
# Allowed CORS origins (comma-separated)
# For production, specify your actual domains
ALLOWED_ORIGINS=https://yourdomain.com,https://n8n.yourdomain.com,https://api.yourdomain.com

# ==================== Database Credentials ====================
# MongoDB root credentials
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your_very_strong_mongodb_password_here

# MongoDB Express credentials (for web UI access)
MONGO_EXPRESS_USER=admin
MONGO_EXPRESS_PASSWORD=your_mongo_express_password

# ==================== Cache Credentials ====================
# Redis password
REDIS_PASSWORD=your_strong_redis_password_here

# Redis Commander credentials (for web UI access)  
REDIS_COMMANDER_USER=admin
REDIS_COMMANDER_PASSWORD=your_redis_commander_password

# ==================== n8n Configuration ====================
# n8n basic auth credentials
N8N_USER=admin
N8N_PASSWORD=your_strong_n8n_password_here

# ==================== SSL/TLS Configuration ====================
# For production with reverse proxy (Nginx, Traefik, etc.)
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/private.key

# ==================== Monitoring & Alerts ====================
# Optional: Webhook URL for health check alerts
# HEALTH_CHECK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url

# Optional: Email configuration for alerts
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# ALERT_EMAIL=admin@yourdomain.com

# ==================== Backup Configuration ====================
# Optional: S3-compatible backup storage
# BACKUP_S3_ENDPOINT=https://s3.amazonaws.com
# BACKUP_S3_ACCESS_KEY=your_s3_access_key
# BACKUP_S3_SECRET_KEY=your_s3_secret_key
# BACKUP_S3_BUCKET=kaayaan-strategist-backups

# ==================== Performance Tuning ====================
# Optional: Custom memory limits
# NODE_OPTIONS=--max-old-space-size=2048

# Optional: Custom worker threads
# UV_THREADPOOL_SIZE=4

# ==================== Development Overrides ====================
# Only for development/staging - remove in production
# DEBUG=mcp:*
# DISABLE_RATE_LIMITING=false
# ENABLE_REQUEST_LOGGING=true